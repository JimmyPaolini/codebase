// ♟️ Constants

/** The directories a workspace project may live in. */
export const WORKSPACE_SCOPES = ["applications", "packages", "tools"];

/** The root README this check reads and enforces project coverage against. */
export const ROOT_README_PATH = "README.md";
