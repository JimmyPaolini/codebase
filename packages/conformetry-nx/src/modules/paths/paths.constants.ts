// ♟️ Constants

/** Input naming the project a generator writes into. */
export const PROJECT_INPUT_NAME = "project";

/** Input naming an existing module a generator writes into. */
export const MODULE_INPUT_NAME = "module";

/** Input naming the workspace directory a new project belongs in. */
export const TYPE_INPUT_NAME = "type";

/** Input overriding path discovery with an explicit destination. */
export const DIRECTORY_INPUT_NAME = "directory";
