{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "implicitDependencies": ["callidescope-cli"],
  "name": "callidescope-examples",
  "projectType": "library",
  "sourceRoot": "packages/callidescope-examples",
  "tags": [
    "framework:nestjs",
    "language:typescript",
    "name:callidescope-examples",
    "type:package"
  ],
  "targets": {
    "codometer": {},
    "dependency-cruiser": {},
    "examples": {
      "cache": true,
      "configurations": {
        "check": {
          "command": "node --import @swc-node/register/esm-register packages/callidescope-cli/src/main.ts callidescope --directories packages/callidescope-examples --config packages/callidescope-examples/callidescope.config.ts --check reports"
        },
        "write": {
          "command": "node --import @swc-node/register/esm-register packages/callidescope-cli/src/main.ts callidescope --directories packages/callidescope-examples --config packages/callidescope-examples/callidescope.config.ts --write"
        }
      },
      "defaultConfiguration": "check",
      "description": "Trace this package's fixtures and gate the committed reports against them. Named `examples` rather than `callidescope` so every examples package in this workspace runs the same way, and so it is never confused with `codebase:callidescope`, which is its mirror image: the workspace target runs `--check depth` because a report there goes stale on nearly every change, while the fixtures traced here are frozen, so a stale report means a fixture, the resolver, or one of the three dependency packages this run's closure reaches moved \u2014 which is exactly the regression this package exists to catch. Those three packages' sources are named in `inputs` alongside the toolchain, because the closure measures them and their callables reach the committed reports, so a cached result is never replayed over a change to them. Their READMEs are not named, because this run publishes a `## \ud83d\udd2d Callidescope` section only for the project it is scoped to. `{workspaceRoot}/configuration/*.config.ts` is named for a different reason and is not redundant with `shared-globals`, which holds `configuration/tsconfig.json` and nothing else: two of those three packages are in the closure only because this package's own `eslint.config.ts` and `codometer.config.ts` spread the root ones, and the root ones import `@codebase/logger/eslint` and `@codometer/configuration`. Drop an import there and the committed reports lose a whole project while every other input is untouched, so a cached green replays over exactly the drift `--check reports` exists to catch. The glob rather than those three files by name, because a closure is whatever the compiler read and a hand-written list of it goes stale in silence \u2014 which is the failure this input exists to prevent. It is a per-target input and never belongs in `shared-globals`, where a high-churn glob once cost the whole workspace every cache hit it had. Depth is not gated here at all: the deep fixtures are meant to breach, which is why `configuration/.callidescopeignore` keeps them out of the workspace run.",
      "executor": "nx:run-commands",
      "inputs": [
        "{projectRoot}/**/*.ts",
        "{projectRoot}/output/**",
        "{projectRoot}/README.md",
        "{workspaceRoot}/configuration/*.config.ts",
        "{workspaceRoot}/packages/callidescope-cli/src/**/*.ts",
        "{workspaceRoot}/packages/callidescope-configuration/src/**/*.ts",
        "{workspaceRoot}/packages/callidescope-graph/src/**/*.ts",
        "{workspaceRoot}/packages/callidescope-output/src/**/*.ts",
        "{workspaceRoot}/packages/codometer-configuration/src/**/*.ts",
        "{workspaceRoot}/packages/logger/src/**/*.ts",
        "shared-globals"
      ],
      "options": {
        "cwd": "{workspaceRoot}"
      },
      "outputs": []
    },
    "knip": {},
    "lint-codebase": {},
    "markdown-lint": {},
    "oxfmt": {},
    "oxlint": {},
    "prettier": {},
    "spell-check": {},
    "test-coverage": {},
    "type-coverage": {},
    "typecheck": {},
    "yaml-lint": {}
  }
}
