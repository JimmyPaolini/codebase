// ♟️ Constants

/** Extensions parsed as JavaScript rather than TypeScript. */
export const JAVASCRIPT_EXTENSIONS = new Set([".cjs", ".js", ".jsx", ".mjs"]);

/** Extensions parsed with JSX syntax enabled. */
export const JSX_EXTENSIONS = new Set([".jsx", ".tsx"]);
