// ♟️ Constants

/** Rendered in place of a diagram for a workspace with no internal file imports at all. */
export const FILE_IMPORTS_WORKSPACE_GRAPH_UNCONNECTED =
  "_This workspace has no internal file imports, in any project._";

/** Header declaring the mermaid diagram type and its default layout direction. */
export const FILE_IMPORTS_WORKSPACE_GRAPH_MERMAID_HEADER = "graph LR";

/** Separates a project name from a file's project-relative path in a qualified node name. */
export const FILE_IMPORTS_WORKSPACE_GRAPH_QUALIFIER_SEPARATOR = "/";
