// ♟️ Constants

/** Separator between a template's aliases when rendered. */
export const ALIAS_SEPARATOR = ", ";

/** Indent for a top-level entry. */
export const ENTRY_INDENT = "  ";

/** Indent for an entry's detail lines. */
export const DETAIL_INDENT = "    ";

/** Indent for a pairing nested beneath a detail line. */
export const PAIRING_INDENT = "      ";

/** Heading above the instances a template explains. */
export const INSTANCES_HEADING = "Instances:";

/** Heading above the templates that explain an instance. */
export const TEMPLATES_HEADING = "Templates:";

/** Label prefixing a template's folder. */
export const TEMPLATE_LABEL = "Template: ";

/** Multiplier turning a 0-to-1 ratio into a percentage. */
export const PERCENT_SCALE = 100;
