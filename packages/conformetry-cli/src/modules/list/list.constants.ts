// ♟️ Constants

/** Separator between a generator's aliases in the readable listing. */
export const ALIAS_SEPARATOR = ", ";

/** Indent for a generator's description and template. */
export const DETAIL_INDENT = "    ";

/** Indent for a generator's name. */
export const GENERATOR_INDENT = "  ";

/** Indent width for the machine-readable listing. */
export const JSON_INDENT = 2;

/** Reported when the configuration declares nothing to list. */
export const NO_GENERATORS_MESSAGE =
  "No generators are declared in the configuration.";

/** Label prefixing a generator's template folder. */
export const TEMPLATE_LABEL = "Template: ";
