// ♟️ Constants

/** Reported when the configuration declares nothing to list. */
export const NO_TEMPLATES_MESSAGE =
  "No templates are declared in the configuration.";

/** Reported when a path filter matches no template. */
export const NO_MATCHES_MESSAGE =
  "No template explains those paths. Either they are not generated code, or they have drifted past recognition.";

/** Heading above the instances a template explains. */
export const INSTANCES_HEADING = "Instances:";

/** Label prefixing a template's folder. */
export const TEMPLATE_LABEL = "Template: ";
