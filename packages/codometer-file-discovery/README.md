## Project Graph

Where this project sits in the Nx project graph: what it depends on, and what depends on it. Regenerated by `nx run synchronization:nx-project-graphs:write`.

<!-- nx-project-graph-start -->

```mermaid
flowchart LR
  codometer_cli["codometer-cli"]
  codometer_file_discovery["codometer-file-discovery"]
  logger["logger"]
  codometer_cli --> codometer_file_discovery
  codometer_file_discovery --> logger
  classDef subject stroke-width:3px
  class codometer_file_discovery subject
```

<!-- nx-project-graph-end -->

## Module Graph

The modules this project defines and the imports between them, published by `nx run synchronization:nestjs-module-graphs:write`.

<!-- nestjs-module-graph-start -->

```mermaid
flowchart LR
  subgraph group0["codometer-file-discovery"]
    FileDiscoveryModule
  end
  subgraph group1["logger"]
    LoggerModule
  end
  FileDiscoveryModule --> LoggerModule
```

<!-- nestjs-module-graph-end -->

## Test

```bash
nx run codometer-file-discovery:vitest
```
