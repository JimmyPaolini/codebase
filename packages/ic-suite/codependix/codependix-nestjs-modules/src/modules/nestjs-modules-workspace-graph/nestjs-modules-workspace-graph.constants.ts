// ♟️ Constants

/** Rendered in place of a diagram for a workspace with no NestJS modules at all. */
export const NESTJS_MODULES_WORKSPACE_GRAPH_UNCONNECTED =
  "_This workspace has no NestJS modules, in any project._";

/** Header declaring the mermaid diagram type and its default layout direction. */
export const NESTJS_MODULES_WORKSPACE_GRAPH_MERMAID_HEADER = "graph LR";

/** Separates a project name from a module's class name in a qualified node name. */
export const NESTJS_MODULES_WORKSPACE_GRAPH_QUALIFIER_SEPARATOR = "/";
