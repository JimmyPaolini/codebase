// ♟️ Constants

/** Error message logged when fewer than two positional arguments are provided. */
export const PATH_MISSING_ARGUMENTS_ERROR =
  'The path command requires both <from> and <to> arguments, as in "codependix path <from> <to>".';
