// ♟️ Constants

/** Filename Nx configures a project with. */
export const PROJECT_CONFIGURATION_FILENAME = "project.json";

/** Filename listing the paths Nx excludes from project discovery. */
export const NX_IGNORE_FILENAME = ".nxignore";

/** Directory the walk never descends into, however a workspace is laid out. */
export const DEPENDENCIES_DIRECTORY_NAME = "node_modules";
