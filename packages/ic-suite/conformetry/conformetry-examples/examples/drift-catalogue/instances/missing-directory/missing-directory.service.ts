// 🎯 Service

/** Does the missing-directory work. */
export class MissingDirectoryService {
  // 🌎 Public Methods

  /** Runs the work. */
  public run(): string {
    return missingDirectoryName;
  }
}
