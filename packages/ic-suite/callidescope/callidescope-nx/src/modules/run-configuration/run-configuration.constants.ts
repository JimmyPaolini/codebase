// ♟️ Constants

/**
 * Filename this plugin's own registration is written in.
 *
 * Read for the `configurationPath` an executor was not given: a workspace that
 * registered the plugin with one has already said where its limits live, and
 * asking the registration is what keeps a target from needing to repeat it.
 */
export const NX_CONFIGURATION_FILENAME = "nx.json";
