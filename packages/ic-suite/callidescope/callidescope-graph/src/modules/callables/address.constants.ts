// ♟️ Constants

/** Matches the digits-only suffix that marks a `:<line>` disambiguator. */
export const DIGITS_ONLY_PATTERN = /^\d+$/u;

/** Matches a `:<line>` disambiguator already on the end of an address. */
export const LINE_DISAMBIGUATOR_PATTERN = /:\d+$/u;
