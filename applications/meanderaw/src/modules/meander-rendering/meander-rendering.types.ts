// 🏷️ Types

/** One lattice point's position on the canvas, in pixels, as `MeanderRenderingService` draws from it. */
export interface MeanderCanvasPoint {
  readonly x: number;
  readonly y: number;
}
