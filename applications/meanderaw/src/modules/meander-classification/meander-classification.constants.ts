// ♟️ Constants

// 🌱 No constants of its own. Each family's defining combination is a
// predicate rather than a value, so the ten of them live in
// `MeanderClassificationService.rules` beside the helpers they read — the
// same place `MosaicNamingService` keeps the eight sub-family rules, and for
// the same reason. The one value a rule does read, each family's own
// structural minimum row count, is `STRUCTURAL_MINIMUM_ROWS`: the record the
// command line already validates against, read rather than restated here.
