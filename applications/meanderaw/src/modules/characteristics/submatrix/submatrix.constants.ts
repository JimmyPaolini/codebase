import type { MatrixPointArm } from "../characteristics.types";

// ♟️ Constants

/** Every direction a point's ink can leave it by. */
export const MATRIX_POINT_ARMS: readonly MatrixPointArm[] = [
  "east",
  "north",
  "south",
  "west",
];
