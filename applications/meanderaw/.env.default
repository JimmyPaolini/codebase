# 🌱 Environment

# Uncomment to bound the sweep below today's unconfigured defaults: the edge
# budget alone (16), and rows/columns unbounded.
# SWEEP_EDGE_BUDGET=16
# SWEEP_MAXIMUM_ROWS=4
# SWEEP_MAXIMUM_COLUMNS=4
