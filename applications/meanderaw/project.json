{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "meanderaw",
  "projectType": "application",
  "sourceRoot": "applications/meanderaw",
  "tags": [
    "domain:meanderaw",
    "framework:nest-commander",
    "framework:nestjs",
    "generator:nestjs-command-project",
    "language:typescript",
    "type:application"
  ],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "options": {
        "assets": [
          {
            "glob": "README.md",
            "input": "applications/meanderaw",
            "output": "."
          }
        ],
        "main": "applications/meanderaw/src/main.ts",
        "outputPath": "dist/applications/meanderaw",
        "tsConfig": "applications/meanderaw/tsconfig.build.json"
      },
      "outputs": ["{options.outputPath}"]
    },
    "codometer": {},
    "dependency-cruiser": {},
    "generate": {
      "cache": false,
      "description": "Generate one meander SVG into output/. `build` compiles this project rather than drawing anything, so writing SVGs is this target's job and the full sweep stays on `start`.",
      "executor": "nx:run-commands",
      "options": {
        "command": "node --import @swc-node/register/esm-register src/main.ts generate {args}",
        "cwd": "{projectRoot}"
      }
    },
    "knip": {},
    "lint-codebase": {},
    "make-projects": {},
    "markdown-lint": {},
    "oxfmt": {},
    "oxlint": {},
    "prettier": {},
    "repl": {
      "cache": false,
      "description": "Start an interactive NestJS REPL for Meanderaw services",
      "executor": "nx:run-commands",
      "options": {
        "command": "node --import @swc-node/register/esm-register src/repl.ts",
        "cwd": "{projectRoot}"
      }
    },
    "spell-check": {},
    "start": {
      "cache": false,
      "description": "Start the Meanderaw application, which with no arguments runs the start command's full sweep",
      "executor": "nx:run-commands",
      "options": {
        "command": "node --import @swc-node/register/esm-register src/main.ts {args}",
        "cwd": "{projectRoot}"
      }
    },
    "test-coverage": {},
    "type-coverage": {},
    "typecheck": {},
    "yaml-lint": {}
  }
}
