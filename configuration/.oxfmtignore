!**/*.d.ts.map
**/*.d.ts
**/.venv
**/*.md
**/*.mdx
**/.next
**/.nx
**/.terraform/**
**/build
**/coverage
**/dist
**/helm/**/templates/**
**/node_modules
**/out
**/routeTree.gen.ts
**/tmp
*.log
*.min.css
*.min.js
*.tmp
.git
.idea
.next
.nx
.nyc_output
.vscode
build
CHANGELOG.md
coverage
dist
**/infrastructure/helm/**/templates/**
node_modules
out
package-lock.json
**/packages/lexico-components/src/components/**
**/packages/lexico-components/src/hooks/**
**/packages/lexico-components/src/lib/**
**/applications/lexico-ingestion/data/**
**/applications/lexico-ingestion/data/library/**
**/applications/lexico-ingestion/src/modules/literature/literature.constants.ts
**/applications/lexico-ingestion/src/modules/library/library.constants.ts
**/library.json
pnpm-lock.yaml
tmp
**/conformetry-templates/**
# Conformetry example fixtures: a template file holds mustache where an
# identifier belongs, so it is not parseable as the language its extension
# names. Each example's own conformetry.config.ts is formatted normally.
**/packages/conformetry-examples/examples/*/instances/**
**/packages/conformetry-examples/examples/*/templates/**
# Emitted from the conformetry configuration; the emitter's output is
# authoritative, and reformatting it makes every conformetry command fail the
# drift check after a `format` run.
**/.conformetry/**
# Callidescope writes this report and then checks the file back against what a
# fresh run would produce, byte for byte. Reformatting it makes
# `callidescope-examples:callidescope` report it stale the moment `format` runs
# — the same failure mode as the emitted conformetry plugin above.
**/packages/callidescope-examples/output/**
# The meander sweep writes `output/index.html`, and the page is committed the
# way the drawings beside it are. `DrawIndexService` renders it compact — one
# line per drawing — and oxfmt expands it to ten times the lines, so the very
# next `meanderaw:start` writes the compact form back and `oxfmt --check` fails
# against what `--write` just produced. The same standoff as the two entries
# above, and `.prettierignore` already excludes this directory for its own
# formatter.
**/applications/meanderaw/output/**
**/data/wikipedia
**/data/wiktionary
yarn.lock

# 🕸️ Every dependency graph codependix writes as JSON. It renders them with
# `JSON.stringify(…, 2)`, which puts one array element per line; oxfmt collapses
# a short array onto one. Reformatting one therefore makes the very next
# `codependix map --check` fail against what `codependix map --write` just wrote — the
# same conflict this file already resolves for `.conformetry/**`, and the same
# reason `configuration/eslint.config.ts` turns `jsonc/sort-array-values` off
# for exactly these files.
**/codependix-*graph.json
