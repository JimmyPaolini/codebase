# Files codometer does not measure, in gitignore syntax.
#
# Untracked files need no mention: discovery enumerates through `git ls-files`,
# so everything .gitignore covers is already absent. What belongs here is the
# opposite — files that are committed but nobody wrote.

# 🔒 Lock files
pnpm-lock.yaml
uv.lock
skills-lock.json

# 🏭 Generated by tooling
/CHANGELOG.md
openwiki/
**/routeTree.gen.ts
projectStructure.cache.json

# 🧩 Vendored by shadcn, not authored here
packages/lexico-components/src/components/
packages/lexico-components/src/hooks/
packages/lexico-components/src/lib/

# 📚 Ingested corpus and generated output rather than authored source. They are
# committed, so `git ls-files` reports them, and they are overwhelmingly
# markdown: left in, they supply 88% of the repository's markdown files and bury
# every prose metric under the data payload. `markdownlint` and `cspell` already
# ignore both for the same reason.
applications/lexico-ingestion/data/
applications/affirmations/output/
applications/lexico-ingestion/src/modules/library/library.constants.ts
applications/lexico-ingestion/src/modules/literature/literature.constants.ts
**/library.json

# 📓 Scratch notes and generator templates
notepads/
templates/

# 📊 Codometer's own output target. Measuring it makes the report an input to
# itself: every badge the last run wrote counts toward the next run's markdown
# metrics, so a written block is stale the moment it lands.
/README.md
